spring:
  profiles:
    include:
    - oauth
    - s3
  application:
    name: apd
  datasource:
    url: jdbc:log4jdbc:mariadb://np.unoeyhi.site:3306/mifo?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=UTC
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: mifo
    password: 1234
  security:
    user:
      name: user1
      password: $2a$10$YP7wdCG6hYvKZD9DjVAJUOSwVkJUxuOgnqXko5mAwBha3/us7PKqm
      roles:
      - USER

  jpa:
    hibernate:
      ddl-auto: update # 'update'는 개발 중에만 사용, 운영 환경에서는 'none' 또는 'validate' 권장
    show-sql: true
    properties:
      hibernate: 
        format_sql: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: html
  devtools:
    restart:
      enabled: true
      additional-paths:
      - src/main/java
      - src/main/resources
      - src/main/resources
      - templates
debug: true  # 디버그 활성화     

server:
  servlet:
    encoding:
      force: true
      
    # context-path: /guestbook


